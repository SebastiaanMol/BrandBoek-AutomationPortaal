services:
  app:
    build: .
    container_name: brand-api
    command: ["uv", "run", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "80", "--reload", "--log-level", "debug"]
    ports:
      - 8000:80
    volumes:
      - ./app:/code/app
    env_file:
      - .env
