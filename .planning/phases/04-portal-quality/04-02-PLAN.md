---
phase: 04-portal-quality
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/components/AutomatiseringForm.tsx
  - src/pages/NieuweAutomatiseringPage.tsx
  - src/pages/BewerkAutomatisering.tsx
  - src/pages/AIUpload.tsx
autonomous: true
requirements:
  - QUAL-02

must_haves:
  truths:
    - "src/lib/storage.ts no longer exists in the repository"
    - "src/components/AutomatiseringForm.tsx exists with the same content as the old src/pages/NieuweAutomatisering.tsx"
    - "NieuweAutomatiseringPage.tsx, BewerkAutomatisering.tsx, and AIUpload.tsx all import from @/components/AutomatiseringForm"
    - "TypeScript compiles clean with zero errors after the move"
    - "The full test suite passes — no regressions"
  artifacts:
    - path: "src/components/AutomatiseringForm.tsx"
      provides: "Moved form component — was src/pages/NieuweAutomatisering.tsx"
      min_lines: 20
  key_links:
    - from: "src/pages/NieuweAutomatiseringPage.tsx"
      to: "src/components/AutomatiseringForm.tsx"
      via: "import AutomatiseringForm from '@/components/AutomatiseringForm'"
      pattern: "AutomatiseringForm"
    - from: "src/pages/BewerkAutomatisering.tsx"
      to: "src/components/AutomatiseringForm.tsx"
      via: "import AutomatiseringForm from '@/components/AutomatiseringForm'"
      pattern: "AutomatiseringForm"
    - from: "src/pages/AIUpload.tsx"
      to: "src/components/AutomatiseringForm.tsx"
      via: "import AutomatiseringForm from '@/components/AutomatiseringForm'"
      pattern: "AutomatiseringForm"
---

<objective>
Remove dead code (storage.ts) and move the misplaced form component to its correct location in components/.

Purpose: Resolves QUAL-02 — eliminates the orphaned localStorage layer and fixes the architectural confusion of a reusable form component living in pages/.
Output: storage.ts deleted, NieuweAutomatisering.tsx moved to src/components/AutomatiseringForm.tsx, three callers updated, TypeScript clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Exact import lines in the three callers — confirmed by grep. -->

Current import in NieuweAutomatiseringPage.tsx (line 3):
```typescript
import NieuweAutomatisering from "./NieuweAutomatisering";
```
Usage (line 21): `<NieuweAutomatisering />`

Current import in BewerkAutomatisering.tsx (line 3):
```typescript
import NieuweAutomatisering from "./NieuweAutomatisering";
```
Usage (line 24): `<NieuweAutomatisering`  (with props — read file to confirm full props)

Current import in AIUpload.tsx (line 4):
```typescript
import NieuweAutomatisering from "./NieuweAutomatisering";
```
Usage (line 753): `<NieuweAutomatisering prefill={prefill} />`

After move, all three become:
```typescript
import AutomatiseringForm from "@/components/AutomatiseringForm";
```
And all JSX references change from `<NieuweAutomatisering` to `<AutomatiseringForm`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete storage.ts (orphaned localStorage layer)</name>
  <files>src/lib/storage.ts</files>
  <read_first>
    - src/lib/storage.ts — read to confirm content before deleting
  </read_first>
  <action>
    Delete the file src/lib/storage.ts.

    This file has ZERO imports anywhere in src/ (confirmed by research grep). It is the old localStorage storage layer, fully superseded by src/lib/supabaseStorage.ts. No callers exist — deletion is safe with no cascading changes required.

    Use the Bash tool to delete: `rm "src/lib/storage.ts"`

    After deletion, run `npx tsc --noEmit` to confirm zero TypeScript errors.
  </action>
  <verify>
    <automated>cd "C:/Users/SebastiaanMol/Desktop/Nieuwe map/automation-navigator" && npx tsc --noEmit 2>&1 && echo "TSC CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - ls src/lib/storage.ts returns "No such file" — file is gone
    - npx tsc --noEmit exits with zero errors
    - npx vitest run — all 54 tests still pass
  </acceptance_criteria>
  <done>src/lib/storage.ts deleted, TypeScript compiles clean, test suite still green.</done>
</task>

<task type="auto">
  <name>Task 2: Move NieuweAutomatisering.tsx to components/AutomatiseringForm.tsx and update 3 callers</name>
  <files>
    src/components/AutomatiseringForm.tsx
    src/pages/NieuweAutomatiseringPage.tsx
    src/pages/BewerkAutomatisering.tsx
    src/pages/AIUpload.tsx
  </files>
  <read_first>
    - src/pages/NieuweAutomatisering.tsx — read full file content before moving
    - src/pages/NieuweAutomatiseringPage.tsx — read to see full import and all usage sites
    - src/pages/BewerkAutomatisering.tsx — read to see full import and all usage sites
    - src/pages/AIUpload.tsx — read to see full import and all usage sites (line 4 import, line 753 usage)
  </read_first>
  <action>
    Step 1 — Copy content to new location:
    Read the full contents of src/pages/NieuweAutomatisering.tsx.
    Write the identical content to src/components/AutomatiseringForm.tsx.
    The default export name inside the file can remain `NieuweAutomatisering` OR be renamed to `AutomatiseringForm` — either works because callers use the default import. Renaming is preferred for clarity but not required for correctness.

    Step 2 — Delete the old file:
    Delete src/pages/NieuweAutomatisering.tsx.

    Step 3 — Update NieuweAutomatiseringPage.tsx:
    Replace line 3:
      `import NieuweAutomatisering from "./NieuweAutomatisering";`
    With:
      `import AutomatiseringForm from "@/components/AutomatiseringForm";`
    Replace all JSX references `<NieuweAutomatisering` with `<AutomatiseringForm` and `</NieuweAutomatisering>` with `</AutomatiseringForm>`.

    Step 4 — Update BewerkAutomatisering.tsx:
    Replace line 3:
      `import NieuweAutomatisering from "./NieuweAutomatisering";`
    With:
      `import AutomatiseringForm from "@/components/AutomatiseringForm";`
    Replace all JSX references `<NieuweAutomatisering` with `<AutomatiseringForm` and closing tags accordingly.

    Step 5 — Update AIUpload.tsx:
    Replace line 4:
      `import NieuweAutomatisering from "./NieuweAutomatisering";`
    With:
      `import AutomatiseringForm from "@/components/AutomatiseringForm";`
    Replace line 753 usage `<NieuweAutomatisering prefill={prefill} />` with `<AutomatiseringForm prefill={prefill} />`.

    Step 6 — Verify:
    Run `npx tsc --noEmit` — must be zero errors.
    Run `npx vitest run` — must remain at 54+ passing tests.
  </action>
  <verify>
    <automated>cd "C:/Users/SebastiaanMol/Desktop/Nieuwe map/automation-navigator" && npx tsc --noEmit 2>&1 && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "NieuweAutomatisering" src/ — returns ONLY the internal export name in AutomatiseringForm.tsx (no import references remaining)
    - ls src/pages/NieuweAutomatisering.tsx — "No such file"
    - ls src/components/AutomatiseringForm.tsx — file exists
    - grep "AutomatiseringForm" src/pages/NieuweAutomatiseringPage.tsx — import present
    - grep "AutomatiseringForm" src/pages/BewerkAutomatisering.tsx — import present
    - grep "AutomatiseringForm" src/pages/AIUpload.tsx — import present
    - npx tsc --noEmit exits clean (zero errors)
    - npx vitest run — 54+ tests pass
  </acceptance_criteria>
  <done>
    src/components/AutomatiseringForm.tsx exists with form component content.
    src/pages/NieuweAutomatisering.tsx deleted.
    All three callers import from @/components/AutomatiseringForm.
    TypeScript compiles clean. Test suite green.
  </done>
</task>

</tasks>

<verification>
After both tasks:
```
npx tsc --noEmit
npx vitest run
```
Expected: zero TypeScript errors, 54+ tests passing.
Also confirm: `grep -r "lib/storage" src/` returns nothing and `grep -r "NieuweAutomatisering" src/pages/` returns nothing.
</verification>

<success_criteria>
- src/lib/storage.ts does not exist
- src/components/AutomatiseringForm.tsx exists
- src/pages/NieuweAutomatisering.tsx does not exist
- grep -r "from.*NieuweAutomatisering" src/ returns zero results
- npx tsc --noEmit clean
- npx vitest run passes all prior tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-portal-quality/04-02-SUMMARY.md`
</output>
