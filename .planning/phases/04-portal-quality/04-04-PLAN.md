---
phase: 04-portal-quality
plan: 04
type: execute
wave: 2
depends_on:
  - 04-02
  - 04-03
files_modified: []
autonomous: false
requirements:
  - QUAL-01

must_haves:
  truths:
    - "All 8 sidebar nav pages load without console errors in the browser"
    - "The /instellingen page (bottom nav) also loads without errors"
    - "No broken UI or blank screens on any nav-linked page"
  artifacts: []
  key_links: []
---

<objective>
Human verification that all sidebar nav pages are stable and accessible — satisfying QUAL-01.

Purpose: Confirms the portal is handover-ready from a navigation stability standpoint. No code changes are expected in this plan; this is a browser-based verification gate.
Output: Human confirmation that all 8 nav pages load cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all sidebar nav pages load without errors</name>
  <files>— (no files modified; this is a browser verification task)</files>
  <read_first>
    - src/components/AppLayout.tsx — to confirm the 8 nav items in scope
  </read_first>
  <what-built>
    Dead code removed (storage.ts deleted, NieuweAutomatisering.tsx moved to components/).
    Domain logic tests passing.
    Portal should be in a clean, stable state.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Open the portal in the browser (typically http://localhost:5173)
    3. Open DevTools (F12) → Console tab
    4. Click each sidebar nav item and verify: page loads, no red console errors appear.

    Pages to check (8 nav items + 1 bottom nav):
    - `/` (Dashboard) — default landing page
    - `/nieuw` (Nieuwe Automatisering) — uses AutomatiseringForm after QUAL-02 move
    - `/alle` (Alle Automatiseringen)
    - `/verificatie` (Verificatie)
    - `/processen` (Processenkaart / canvas)
    - `/analyse` (Analyse)
    - `/imports` (Imports)
    - `/instellingen` (bottom nav — Instellingen)

    OUT OF SCOPE — do NOT check these hidden routes (commented out from nav):
    - /mindmap, /kennisgraaf, /bpmn, /proceskaart

    Expected per page: page renders visible content, DevTools console shows zero red errors.
    Network errors for missing data (404 on Supabase rows) are acceptable — UI errors are not.

    Specifically confirm `/nieuw` renders the AutomatiseringForm correctly after the QUAL-02 move.
  </how-to-verify>
  <action>
    No automated action for this task. Claude prepares by running `npm run dev` to confirm the dev server starts, then pauses for human browser verification of all 8 nav pages.

    Before pausing: run `npm run dev` and confirm it starts without error.
    Pre-check: run `npx tsc --noEmit` to confirm build is clean before asking user to verify.
  </action>
  <verify>
    <automated>cd "C:/Users/SebastiaanMol/Desktop/Nieuwe map/automation-navigator" && npx tsc --noEmit 2>&1 && echo "BUILD CLEAN — ready for human nav verification"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 nav pages render visible content
    - Zero JavaScript errors in the browser console on any of the 8 pages
    - /nieuw page loads the form (AutomatiseringForm renders correctly after move)
  </acceptance_criteria>
  <done>Human has verified all 8 sidebar nav pages load without console errors and typed "approved".</done>
  <resume-signal>Type "approved" if all 8 pages load cleanly, or describe which page(s) show errors.</resume-signal>
</task>

</tasks>

<verification>
Human confirms all 8 sidebar nav pages load without console errors. Build must be clean (tsc --noEmit) before human verification begins.
</verification>

<success_criteria>
- Human has clicked all 8 nav items
- Zero JavaScript console errors observed on any page
- /nieuw page specifically confirmed working after QUAL-02 file move
- User types "approved" to unblock phase completion
</success_criteria>

<output>
After completion, create `.planning/phases/04-portal-quality/04-04-SUMMARY.md`
</output>
