---
phase: 04-portal-quality
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/test/domainLogic.test.ts
autonomous: true
requirements:
  - QUAL-03

must_haves:
  truths:
    - "domainLogic.test.ts exists with failing stubs for berekenComplexiteit, berekenImpact, and detectProblems"
    - "Running the test file produces RED results — tests fail because assertions are not yet implemented"
    - "The test file imports directly from @/lib/types and @/lib/graphProblems without duplication"
  artifacts:
    - path: "src/test/domainLogic.test.ts"
      provides: "Failing test scaffold for QUAL-03 domain logic coverage"
      exports: []
      min_lines: 40
  key_links:
    - from: "src/test/domainLogic.test.ts"
      to: "src/lib/types.ts"
      via: "import { berekenComplexiteit, berekenImpact } from '@/lib/types'"
      pattern: "berekenComplexiteit|berekenImpact"
    - from: "src/test/domainLogic.test.ts"
      to: "src/lib/graphProblems.ts"
      via: "import { detectProblems } from '@/lib/graphProblems'"
      pattern: "detectProblems"
---

<objective>
Create a failing test scaffold for the three core domain logic functions that need coverage in QUAL-03.

Purpose: Establishes the RED state required before Plan 03 implements the passing tests. Confirms the import paths work and the test infrastructure is wired correctly.
Output: src/test/domainLogic.test.ts with stub `it.todo` or failing assertions for berekenComplexiteit, berekenImpact, and detectProblems.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/types.ts
@src/lib/graphProblems.ts
</context>

<interfaces>
<!-- Key types the executor needs. Extracted directly from the codebase. -->

From src/lib/types.ts:
```typescript
export interface Automatisering {
  id: string;
  naam: string;
  categorie: Categorie;
  doel: string;
  trigger: string;
  systemen: Systeem[];
  stappen: string[];
  afhankelijkheden: string;
  owner: string;
  status: Status;
  verbeterideeën: string;
  mermaidDiagram: string;
  koppelingen: Koppeling[];
  fasen: KlantFase[];
  createdAt: string;
  laatstGeverifieerd: string | null;
  geverifieerdDoor: string;
  externalId?: string;
  source?: string;
  lastSyncedAt?: string | null;
  branches?: AutomationBranch[];
}

export function berekenComplexiteit(a: Automatisering): number
// stappenScore = stappen.length * 10, capped at 40
// systemenScore = systemen.length * 12, capped at 36
// afhankelijkhedenScore = 15 if non-empty string, else 0
// koppelingenScore = koppelingen.length * 5, capped at 15
// total capped at 100

export function berekenImpact(a: Automatisering, alle: Automatisering[]): number
// fasenScore = fasen.length * 12
// systemenScore = systemen.length * 8
// depScore = (number of automations whose koppelingen[].doelId === a.id) * 20
// statusBonus = 10 if status === "Actief"
// total capped at 100
```

From src/lib/graphProblems.ts:
```typescript
export type ProblemType =
  "orphan" | "missing-owner" | "missing-trigger" |
  "missing-systems" | "outdated" | "unverified" | "no-goal" | "broken-link"

export type Severity = "error" | "warning" | "info"

export interface GraphProblem {
  id: string
  automationId: string
  type: ProblemType
  severity: Severity
  message: string
  suggestion: string
}

export function detectProblems(automations: Automatisering[]): GraphProblem[]
// Orphan detection: any automation with zero edges in the adjacency graph
// Pitfall: a single automation with empty koppelingen IS flagged as orphan
// Pitfall: berekenImpact needs a populated `alle` array to get non-zero depScore
```

Fixture helper pattern (established across prior test files — reuse exactly):
```typescript
function makeAutomatisering(overrides: Partial<Automatisering> = {}): Automatisering {
  return {
    id: "auto-1", naam: "Test", categorie: "HubSpot Workflow",
    doel: "Test doel", trigger: "Form submitted", systemen: [],
    stappen: [], afhankelijkheden: "", owner: "Jan", status: "Actief",
    verbeterideeën: "", mermaidDiagram: "", koppelingen: [],
    fasen: [], createdAt: "2026-01-01T00:00:00Z",
    laatstGeverifieerd: null, geverifieerdDoor: "",
    ...overrides,
  };
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing test scaffold for domain logic (QUAL-03 Wave 0)</name>
  <files>src/test/domainLogic.test.ts</files>
  <read_first>
    - src/lib/types.ts — to confirm berekenComplexiteit and berekenImpact signatures
    - src/lib/graphProblems.ts — to confirm detectProblems signature and ProblemType values
    - src/test/processCanvas.test.ts — to match existing makeAutomatisering helper pattern
  </read_first>
  <behavior>
    berekenComplexiteit stubs:
    - stub: empty automation returns 0
    - stub: 4 stappen returns 40 (capped)
    - stub: 4+ stappen + 3 systemen + afhankelijkheden returns correct total

    berekenImpact stubs:
    - stub: automation with 2 fasen returns fasenScore component
    - stub: automation with depScore (another automation links to it) returns nonzero depScore
    - stub: Actief status adds statusBonus

    detectProblems stubs:
    - stub: automation with no owner gets missing-owner problem
    - stub: automation with no trigger gets missing-trigger problem
    - stub: single isolated automation (empty koppelingen) gets orphan problem
    - stub: two connected automations — neither is an orphan
  </behavior>
  <action>
    Create src/test/domainLogic.test.ts.

    File structure:
    ```typescript
    import { describe, it, expect } from "vitest";
    import { berekenComplexiteit, berekenImpact, Automatisering } from "@/lib/types";
    import { detectProblems } from "@/lib/graphProblems";

    function makeAutomatisering(overrides: Partial<Automatisering> = {}): Automatisering {
      return {
        id: "auto-1", naam: "Test", categorie: "HubSpot Workflow",
        doel: "Test doel", trigger: "Form submitted", systemen: [],
        stappen: [], afhankelijkheden: "", owner: "Jan", status: "Actief",
        verbeterideeën: "", mermaidDiagram: "", koppelingen: [],
        fasen: [], createdAt: "2026-01-01T00:00:00Z",
        laatstGeverifieerd: null, geverifieerdDoor: "",
        ...overrides,
      };
    }

    describe("berekenComplexiteit", () => {
      it.todo("empty automation returns 0");
      it.todo("4 stappen returns stappenScore of 40");
      it.todo("cap is respected — 5 stappen still returns 40");
      it.todo("afhankelijkheden non-empty adds 15");
    });

    describe("berekenImpact", () => {
      it.todo("2 fasen returns fasenScore of 24");
      it.todo("depScore: automation depended on by another scores 20");
      it.todo("Actief status adds 10 bonus");
    });

    describe("detectProblems", () => {
      it.todo("automation missing owner gets missing-owner problem");
      it.todo("automation missing trigger gets missing-trigger problem");
      it.todo("isolated automation with empty koppelingen is flagged as orphan");
      it.todo("two connected automations have no orphan problem");
    });
    ```

    Use `it.todo(...)` stubs so the file is valid TypeScript and the suite reports todos (not failures that block Wave 1 parallel execution). The Wave 1 plan (04-03) will replace todos with real assertions.
  </action>
  <verify>
    <automated>cd "C:/Users/SebastiaanMol/Desktop/Nieuwe map/automation-navigator" && npx vitest run src/test/domainLogic.test.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - grep "domainLogic.test.ts" src/test/domainLogic.test.ts — file exists
    - grep "berekenComplexiteit" src/test/domainLogic.test.ts — import present
    - grep "berekenImpact" src/test/domainLogic.test.ts — import present
    - grep "detectProblems" src/test/domainLogic.test.ts — import present
    - grep "makeAutomatisering" src/test/domainLogic.test.ts — fixture helper present
    - npx vitest run src/test/domainLogic.test.ts exits without compilation errors
    - npx vitest run (full suite) still shows all 54 prior tests passing
  </acceptance_criteria>
  <done>src/test/domainLogic.test.ts exists, TypeScript-valid, imports from @/lib/types and @/lib/graphProblems, contains describe blocks for all three functions with it.todo stubs, and does not break the existing 54-test suite.</done>
</task>

</tasks>

<verification>
Run full suite to confirm scaffold does not regress prior tests:
```
npx vitest run
```
Expected: 54 existing tests still passing, new todos listed (not failures).
</verification>

<success_criteria>
- src/test/domainLogic.test.ts exists with describe blocks for berekenComplexiteit, berekenImpact, detectProblems
- All imports resolve (tsc --noEmit clean)
- Prior 54-test suite remains passing
- it.todo stubs visible in vitest output
</success_criteria>

<output>
After completion, create `.planning/phases/04-portal-quality/04-01-SUMMARY.md`
</output>
