---
phase: 1
slug: process-canvas
status: draft
shadcn_initialized: true
preset: "style=default, baseColor=slate, cssVariables=true"
created: 2026-03-30
revised: 2026-03-30
---

# Phase 1 — UI Design Contract: Process Canvas

> Visual and interaction contract for Phase 1. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> Source of truth: existing codebase (`src/index.css`, `tailwind.config.ts`, `components.json`).
> This phase is polish + bug-fix, not a rebuild — all contract values reflect established patterns.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui (style: default) | `components.json` |
| Preset | baseColor=slate, cssVariables=true, tsx=true | `components.json` |
| Component library | Radix UI (via shadcn) | `src/components/ui/` — 48 components installed |
| Icon library | lucide-react | `src/pages/Processen.tsx`, 41 files |
| Font | IBM Plex Sans (400, 500, 600, 700) + JetBrains Mono (code/labels) | `src/index.css` |

---

## Visual Hierarchy

The swimlane canvas (`ProcessCanvas.tsx`) is the primary focal point — it occupies the majority of the viewport and is where all user interaction takes place. `AutomationDetailPanel` is the secondary focal point: when open, it draws attention to the selected automation while the canvas recedes to a secondary role. `UnassignedPanel` is tertiary — it is always visible but never competes with the canvas.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding, inline spacing (`gap-1`, `px-1`) |
| sm | 8px | Compact element spacing, panel item padding, section label margin-bottom (`px-2`, `gap-2`, `mb-2`) |
| md | 16px | Default panel padding, form field spacing (`px-4`) |
| lg | 24px | Panel section separation, card padding |
| xl | 32px | Canvas toolbar vertical rhythm |
| 2xl | 48px | Major section breaks (swimlane header height) |
| 3xl | 64px | Page-level spacing (not used in canvas view) |

Exceptions (not spacing tokens — fixed layout values):
- Automation dot diameter: 20px (existing SVG contract, do not change)
- Detail panel width: 320px (`w-80`) — fixed, not a spacing token
- Unassigned panel width: 288px (`w-72`) — fixed, not a spacing token

> **Existing codebase pattern for future remediation:** `AutomationDetailPanel` currently applies `mb-1.5` (6px) to panel section label margin-bottom. This is a non-4px-grid value. It is NOT part of the spacing contract and MUST NOT be introduced in new code. It will be remediated to `mb-2` (8px) in a future polish phase.

---

## Typography

| Role | Size | Weight | Line Height | Font | Source |
|------|------|--------|-------------|------|--------|
| Body | 14px | 400 (regular) | 1.5 | IBM Plex Sans | `text-sm` — established in panel prose |
| Label / metadata | 12px | 400 (regular) | 1.4 | IBM Plex Sans | `text-xs` — tool name, step labels |
| Section header (panel) | 11px | 600 (semibold) | 1.0 | IBM Plex Sans | `.label-uppercase` utility class — canonical section label pattern |
| Name / heading | 14px | 600 (semibold) | snug (1.375) | IBM Plex Sans | `text-sm font-semibold` — automation name in panel header |

Notes:
- Maximum 4 type roles in this phase. No display-size headings needed (canvas is a tool, not a marketing page).
- Badge text: 11px font-bold uppercase — use `.label-uppercase` for all category labels and section headers without exception. This eliminates the former 10px/11px ambiguity: 11px is the single canonical size for all label-scale text.
- The `Section` component in `AutomationDetailPanel` must use `.label-uppercase` (11px). Any existing `text-[10px]` usages in section headers are non-conforming and should be updated to `.label-uppercase` during Phase 1 polish.

---

## Color

| Role | HSL Value | Hex approx. | Usage |
|------|-----------|-------------|-------|
| Dominant (60%) | `210 40% 98%` | `#f8fafc` | Page background (`--background`) |
| Secondary (30%) | `0 0% 100%` / `210 40% 96%` | `#ffffff` / `#f1f5f9` | Card surfaces, panel backgrounds, sidebar-adjacent areas |
| Accent (10%) | `210 100% 40%` | `#0066cc` | See reserved-for list below |
| Destructive | `0 84% 60%` | `#f43f3f` approx. | Destructive actions only (detach confirmation, reset confirmation) |

**Accent reserved for:**
- Save button (primary action) — `bg-primary` (= `--primary: 222 47% 11%`)
- Ring/focus indicator on interactive canvas elements — `--ring: 210 100% 40%`
- Sidebar active nav item highlight — `--sidebar-primary: 210 100% 40%`
- Toast action confirmation link

**Accent is NOT used for:** hover states, badges, status indicators, lane headers, or read-only labels.

**Swimlane lane colors (domain-specific, not part of 60/30/10):**

| Lane | HSL | Usage |
|------|-----|-------|
| marketing | `280 60% 55%` | Marketing swimlane header + automation dot accent |
| sales | `215 80% 52%` | Sales swimlane header + automation dot accent |
| onboarding | `165 60% 42%` | Onboarding swimlane header + automation dot accent |
| boekhouding | `35 85% 55%` | Boekhouding swimlane header + automation dot accent |
| management | `350 65% 55%` | Management/Offboarding swimlane header + automation dot accent |

Lane colors are set via `--lane-*` CSS custom properties. Do not inline hex; always reference the CSS variable.

**Automation dot:** `--dot-automation: 45 95% 55%` (amber-yellow) — used for unattached automation indicators.

**Status badge colors (semantic only — read-only display):**

| Status | Style |
|--------|-------|
| Actief | `bg-emerald-50 text-emerald-700 border-emerald-200` |
| In review | `bg-amber-50 text-amber-700 border-amber-200` |
| Verouderd | `bg-red-50 text-red-700 border-red-200` |
| Uitgeschakeld | `bg-slate-100 text-slate-500 border-slate-200` |

---

## Interaction Contract

### Canvas Interactions (PROC-01, PROC-02)

| Interaction | Trigger | Feedback |
|-------------|---------|----------|
| Drag automation from UnassignedPanel onto a connection arrow | `dragstart` on automation row → `drop` on SVG arrow | Automation dot appears on arrow; automation moves from unassigned to assigned section |
| Drag step node | `mousedown` + `mousemove` on step rect | Step rect follows cursor; canvas redraws connections in real time |
| Draw connection between steps | Click first step → click second step | Arrow drawn between the two step rects with animation |
| Click automation dot | `click` on SVG dot | AutomationDetailPanel opens (slides in from right edge) |
| Hover connection arrow | `mouseenter` on SVG path | Edge glow: `stroke-width: 5`, `filter: drop-shadow(0 0 8px currentColor)` — defined in `.react-flow__edge:hover` CSS |

### Persistence Interactions (PROC-03)

| Interaction | Trigger | Feedback |
|-------------|---------|----------|
| Save canvas | Click "Opslaan" button | `toast.success("Proceskaart opgeslagen")` — Sonner toast, bottom-right |
| Save fails | Supabase error | `toast.error("Opslaan mislukt — controleer de database")` |
| Reset to saved | Click "Terugzetten" → confirm dialog → confirm | `toast.info("Teruggezet naar opgeslagen versie")` |
| Unsaved changes indicator | `isDirty === true` | Save and Reset buttons become enabled (not disabled) |
| Load on mount | Page load | Loading spinner shown while `fetchProcessState()` resolves; canvas hidden until `loading === false` |

### Detail Panel Interactions (PROC-04)

| Interaction | Trigger | Feedback |
|-------------|---------|----------|
| Open detail panel | Click automation dot | Panel slides in from right; canvas shrinks or panel overlaps canvas edge |
| Close detail panel | Click ✕ button | Panel closes; canvas returns to full width |
| Detach automation | Click "Loskoppelen" in panel | Automation removed from arrow; moved back to unassigned list; dirty state set |

### Loading and Error States

| State | When | Visual |
|-------|------|--------|
| Initial load | `loading === true` on mount | Full-canvas skeleton or centered spinner; no empty-state text shown |
| Empty canvas | Canvas loaded but no steps exist | Empty state — see Copywriting Contract |
| Supabase error on load | `fetchProcessState()` rejects | Console error only (existing pattern); canvas falls back to `initialState` |
| Supabase error on save | `saveProcessState()` rejects | `toast.error(...)` — see Copywriting Contract |

> **Existing codebase pattern noted:** `mb-1.5` (6px) is currently applied to panel section label margin-bottom inside `AutomationDetailPanel`. This deviates from the 4px spacing grid. New code must not replicate this pattern. Remediation to `mb-2` (8px) is deferred to a future polish phase.

---

## Component Inventory

Components used or likely needed in this phase (all from `src/components/ui/`):

| Component | Usage |
|-----------|-------|
| `Button` | Save, Reset, Add Step, close panel |
| `Badge` | Status badge in detail panel, tool badge, assigned count |
| `AlertDialog` | Reset confirmation dialog, save confirmation dialog |
| `Skeleton` | Loading state placeholder for canvas area |
| `Sonner` (toast) | Save success/fail, reset feedback |
| `Tooltip` | Icon-only toolbar buttons (Save, Reset) |
| `ScrollArea` | UnassignedPanel list when overflow; detail panel body |
| `Separator` | Section dividers in detail panel |

Canvas itself (`ProcessCanvas.tsx`) is custom SVG — not a shadcn component.

Note: `DropdownMenu` is NOT included in Phase 1. Export capability (PNG / PDF) is scoped to Phase 3 per CONTEXT.md Phase Boundary.

---

## Copywriting Contract

All UI copy follows the project convention: **Dutch labels, English code identifiers**.

| Element | Dutch Copy | Notes |
|---------|-----------|-------|
| Primary CTA (save) | "Opslaan" | Button label; existing in codebase — do not change |
| Secondary CTA (reset) | "Terugzetten" | Button label; existing in codebase — do not change |
| Add step CTA | "Stap toevoegen" | Icon + label; existing pattern |
| Load success (implicit) | — | No toast on successful load; canvas just renders |
| Save success toast | "Proceskaart opgeslagen" | Existing — do not change |
| Save error toast | "Opslaan mislukt — controleer de database" | Existing — do not change |
| Reset feedback toast | "Teruggezet naar opgeslagen versie" | Existing — do not change |
| Empty canvas heading | "Nog geen stappen" | Shown when canvas has zero step nodes |
| Empty canvas body | "Voeg een stap toe om de proceskaart op te bouwen." | Actionable: describes what to do next |
| Empty unassigned panel | "Alle automatiseringen zijn gekoppeld." | Positive framing — no automations waiting |
| Load error (no UI) | — | Error goes to `console.error` only (existing pattern); canvas falls back silently |
| Detach confirmation | none — immediate action | No confirmation dialog; detach is reversible (re-drag) |
| Reset confirmation heading | "Wijzigingen terugzetten?" | AlertDialog title |
| Reset confirmation body | "De canvas wordt teruggezet naar de laatste opgeslagen versie. Niet-opgeslagen wijzigingen gaan verloren." | AlertDialog description |
| Reset confirm button | "Terugzetten" | Destructive variant |
| Reset cancel button | "Annuleren" | Default variant |
| Save confirmation heading | "Proceskaart opslaan?" | AlertDialog title (if confirm dialog used) |
| Save confirmation body | "De huidige layout wordt opgeslagen als de nieuwe versie." | AlertDialog description |
| Save confirm button | "Opslaan" | Primary variant |
| Detach button label | "Loskoppelen" | In detail panel — icon + label |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Badge, AlertDialog, Skeleton, Sonner, Tooltip, ScrollArea, Separator, Card, Sheet | not required |
| Third-party | none | not applicable |

No third-party registries declared for this phase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 01-process-canvas*
*UI-SPEC created: 2026-03-30*
*UI-SPEC revised: 2026-03-30 (blocking issues resolved)*
*Design system source: existing codebase (components.json, tailwind.config.ts, src/index.css)*
