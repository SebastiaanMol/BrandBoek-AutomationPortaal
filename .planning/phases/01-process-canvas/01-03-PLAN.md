---
phase: 01-process-canvas
plan: 03
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - src/components/process/AutomationDetailPanel.tsx
autonomous: true
requirements:
  - PROC-04

must_haves:
  truths:
    - "Section labels in the detail panel use 11px bold uppercase text, consistent with all other label-scale text in the application"
    - "Section label spacing uses the 4px grid (mb-2 = 8px), not the non-conforming mb-1.5 (6px)"
  artifacts:
    - path: "src/components/process/AutomationDetailPanel.tsx"
      provides: "Section component using .label-uppercase utility class"
      contains: "label-uppercase"
  key_links:
    - from: "src/components/process/AutomationDetailPanel.tsx"
      to: "src/index.css"
      via: ".label-uppercase utility class"
      pattern: "label-uppercase"
---

<objective>
Fix the Section component typography in AutomationDetailPanel.tsx to use the canonical `.label-uppercase` CSS utility instead of the non-conforming inline Tailwind classes.

Purpose: PROC-04 requires a working detail panel. The panel functions correctly but deviates from the established design system — section labels use `text-[10px]` (10px) instead of the canonical `text-[11px]` (11px) and `mb-1.5` (6px, off 4px-grid) instead of `mb-2` (8px). Per UI-SPEC §Typography, `.label-uppercase` is the single canonical pattern for all section headers without exception.

Output: AutomationDetailPanel.tsx with `Section` component using `className="label-uppercase mb-2"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-process-canvas/01-UI-SPEC.md
@.planning/phases/01-process-canvas/01-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/index.css (the canonical utility class to use):
```css
.label-uppercase {
  @apply text-[11px] font-bold uppercase tracking-widest text-muted-foreground;
}
```

From src/components/process/AutomationDetailPanel.tsx (lines 24-33, the current non-conforming Section):
```typescript
function Section({ label, children }: { label: string; children: React.ReactNode }) {
  return (
    <div>
      <p className="text-[10px] font-semibold text-muted-foreground uppercase tracking-wider mb-1.5">
        {label}
      </p>
      {children}
    </div>
  );
}
```

Target state (per RESEARCH.md §Pattern 3 and UI-SPEC §Typography):
```typescript
function Section({ label, children }: { label: string; children: React.ReactNode }) {
  return (
    <div>
      <p className="label-uppercase mb-2">
        {label}
      </p>
      {children}
    </div>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Section component with .label-uppercase canonical pattern</name>
  <files>src/components/process/AutomationDetailPanel.tsx</files>
  <read_first>
    - src/components/process/AutomationDetailPanel.tsx
    - src/index.css (lines around .label-uppercase to confirm the class definition)
  </read_first>
  <action>
    Make exactly ONE change to src/components/process/AutomationDetailPanel.tsx.

    Find the `Section` component (lines 24-33). Replace the `<p>` className from:
    ```
    className="text-[10px] font-semibold text-muted-foreground uppercase tracking-wider mb-1.5"
    ```
    to:
    ```
    className="label-uppercase mb-2"
    ```

    The complete `Section` function after the change must be:
    ```typescript
    function Section({ label, children }: { label: string; children: React.ReactNode }) {
      return (
        <div>
          <p className="label-uppercase mb-2">
            {label}
          </p>
          {children}
        </div>
      );
    }
    ```

    Do NOT change anything else in the file. This is a single-class replacement — no logic changes, no import changes, no structural changes.

    Verify that `src/index.css` contains `.label-uppercase` before making the change. If it does not exist, stop and report — do not inline the styles.
  </action>
  <verify>
    <automated>npx vitest run src/test/processCanvas.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "label-uppercase" src/components/process/AutomationDetailPanel.tsx` returns at least 1 match
    - `grep "text-\[10px\]" src/components/process/AutomationDetailPanel.tsx` returns NO match (old class is gone)
    - `grep "mb-1\.5" src/components/process/AutomationDetailPanel.tsx` returns NO match (non-grid spacing is gone)
    - `grep "label-uppercase" src/index.css` returns a match (confirming the utility class exists)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AutomationDetailPanel.tsx Section component uses `label-uppercase mb-2`. The non-conforming `text-[10px]` and `mb-1.5` classes are removed. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `grep "label-uppercase" src/components/process/AutomationDetailPanel.tsx` returns a match
- `grep "text-\[10px\]" src/components/process/AutomationDetailPanel.tsx` returns no match
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Section component uses `.label-uppercase mb-2`
- No remaining `text-[10px]` or `mb-1.5` in AutomationDetailPanel.tsx
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-process-canvas/01-03-SUMMARY.md`
</output>
