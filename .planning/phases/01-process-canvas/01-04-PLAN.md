---
phase: 01-process-canvas
plan: 04
type: execute
wave: 2
depends_on:
  - 01-02
  - 01-03
files_modified: []
autonomous: false
requirements:
  - PROC-01
  - PROC-02
  - PROC-03
  - PROC-04

must_haves:
  truths:
    - "User can open the Processen page and see all automations arranged in swimlanes by customer phase"
    - "User can drag an automation to a connection arrow and its dot appears on the canvas"
    - "After clicking Opslaan and refreshing the page, attached automations are still shown as dots on their arrows"
    - "User can click an automation dot to open the side panel and read trigger, steps, systems, and owner"
    - "Canvas renders without console errors for all five customer phases"
  artifacts:
    - path: "src/pages/Processen.tsx"
      provides: "Fully functional canvas with persistence fix"
      contains: "savedLinksRef"
    - path: "src/components/process/AutomationDetailPanel.tsx"
      provides: "Detail panel with correct typography"
      contains: "label-uppercase"
  key_links:
    - from: "Browser (Processen page)"
      to: "Supabase (process_state table)"
      via: "saveProcessState / fetchProcessState"
      pattern: "Opslaan → refresh → dots still on arrows"
---

<objective>
Human verification of the completed Phase 1 canvas. All four success criteria are tested manually in the browser using real Supabase data.

Purpose: The automated tests cover pure-logic paths, but drag-and-drop, Supabase round-trips, and visual rendering require a real browser. This checkpoint confirms Phase 1 is graduation-demo ready.

Output: Human confirmation that all four PROC requirements pass, or a list of issues to address.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-process-canvas/01-CONTEXT.md
@.planning/phases/01-process-canvas/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm it is green</name>
  <files>src/test/processCanvas.test.ts</files>
  <read_first>
    - src/test/processCanvas.test.ts
  </read_first>
  <action>
    Run the full Vitest suite to confirm all tests pass before handing off to the human checkpoint.

    Command: `npx vitest run`

    If any tests fail, fix them before proceeding to the checkpoint task. Do not proceed to Task 2 with a red test suite.

    Also run TypeScript check: `npx tsc --noEmit`
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0
    - Output contains "Tests" with 0 failed
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Full test suite green. TypeScript compiles without errors. Ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all four Phase 1 success criteria</name>
  <files></files>
  <action>
    Start the dev server (`npm run dev`) and open http://localhost:5173. Navigate to the Processen page and work through all five verification checks below. The human will confirm or report failures.
  </action>
  <what-built>
    - PROC-03 fix: savedLinksRef pattern so autoLinks survive the two-effect load sequence
    - PROC-04 fix: Section component uses .label-uppercase (11px) instead of text-[10px]
    - Wave 0 test scaffold covering all four PROC logic paths
  </what-built>
  <how-to-verify>
    Run the dev server if not already running: `npm run dev`

    Open the app at http://localhost:5173 and navigate to the Processen page.

    **Check 1 — PROC-01: Automations visible in swimlanes**
    - Confirm the UnassignedPanel on the right shows the list of automations from Supabase
    - Confirm each automation is labelled with its name and tool
    - Confirm the five swimlanes (Marketing, Sales, Onboarding, Boekhouding, Management) are all visible
    - Open DevTools Console. Confirm NO red errors appear on page load

    **Check 2 — PROC-02: Drag automations between swimlanes**
    - Drag any automation from the UnassignedPanel and drop it onto a connection arrow in the canvas
    - Confirm a dot appears on the arrow
    - Confirm the automation moves from the unassigned list to the "Gekoppeld" section
    - Drag a second automation to a different arrow

    **Check 3 — PROC-03: Positions persist after refresh (CRITICAL CHECK)**
    - With at least one automation attached to an arrow, click "Opslaan"
    - Confirm the "Proceskaart opgeslagen" toast appears
    - Refresh the page (F5 or Ctrl+R)
    - Confirm the previously attached automations are still shown as dots on their arrows (NOT back in the unassigned list)
    - If all automations reset to unassigned after refresh: the savedLinksRef fix did not work — report this

    **Check 4 — PROC-04: Detail panel shows all fields**
    - Click any automation dot on the canvas
    - Confirm the AutomationDetailPanel opens on the right
    - Confirm these fields are visible (may be absent if the DB record has no data for a field):
      Name and tool at the top, Trigger section, Systemen badges, Stappen numbered list, Owner in Beheer section
    - Confirm section labels are bold uppercase — this is the typography fix

    **Check 5 — Visual sanity**
    - Confirm the detail panel section headers look consistent with other bold uppercase labels in the UI (11px, bold)
    - Confirm no console errors after all interactions
  </how-to-verify>
  <verify>
    <automated>MISSING — manual browser verification required for drag-drop, Supabase round-trips, and visual rendering</automated>
  </verify>
  <done>Human has confirmed all four PROC requirements pass in the browser with real Supabase data.</done>
  <resume-signal>
    Type "approved" if all five checks pass.
    Or describe any failing checks (e.g., "PROC-03 failed — automations reset after refresh") so Claude can create gap closure plans.
  </resume-signal>
</task>

</tasks>

<verification>
- Full Vitest suite green before checkpoint
- Human has confirmed all four PROC requirements in browser
- No console errors on the Processen page
</verification>

<success_criteria>
- PROC-01: Automations visible in swimlanes ✓
- PROC-02: Drag-and-drop works ✓
- PROC-03: Positions persist after save + refresh ✓
- PROC-04: Detail panel shows trigger, steps, systems, owner ✓
- No console errors ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-process-canvas/01-04-SUMMARY.md`
</output>
