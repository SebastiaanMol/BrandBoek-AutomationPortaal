---
phase: 03-export
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified: []
autonomous: false
requirements:
  - PROC-05

must_haves:
  truths:
    - "User can click the Export button on the Processen page and download a PNG"
    - "User can click the Export button on the Processen page and download a PDF"
    - "The exported PNG shows the swimlane layout with automation names legible at normal zoom"
    - "The exported PDF shows the correct landscape layout matching the canvas view"
    - "Swimlane header backgrounds have correct colours (CSS variables resolved, not transparent/grey)"
  artifacts:
    - path: "src/pages/Processen.tsx"
      provides: "Fully working PNG and PDF export"
      exports: []
  key_links:
    - from: "Export dropdown button (Processen header)"
      to: "exportPng() / exportPdf()"
      via: "DropdownMenuItem onClick handlers"
      pattern: "onClick.*exportPng|onClick.*exportPdf"
---

<objective>
Human visual verification that PNG and PDF export works end-to-end in the browser.

Purpose: The automated tests in exportFlow.test.ts confirm pure-logic correctness. The actual rasterisation pipeline (svgToCanvas, CSS variable resolution, jsPDF addImage) can only be verified by a human looking at the downloaded files. This checkpoint gates Phase 3 completion.
Output: Confirmed export works — no code changes expected. If issues found, this plan stays open while bugs are fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-export/03-RESEARCH.md
@.planning/phases/03-export/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and confirm automated tests pass</name>
  <files>src/test/exportFlow.test.ts</files>
  <read_first>
    - .planning/phases/03-export/03-02-SUMMARY.md (confirm Wave 1 completed cleanly)
  </read_first>
  <action>
    Run the full test suite to confirm baseline before handing off to human verification:
    ```
    npx vitest run
    ```
    All tests must be green. If any test is red, do NOT proceed to the checkpoint — fix the
    regression first.

    Then start the dev server so the user can test in the browser:
    ```
    npm run dev
    ```
    The dev server typically starts at http://localhost:5173 (Vite default).
    Confirm the server started successfully and output the URL.
  </action>
  <verify>
    <automated>cd "/c/Users/SebastiaanMol/Desktop/Nieuwe map/automation-navigator" && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` shows all tests passing, 0 failures
    - Dev server starts without build errors
    - Terminal output includes a localhost URL (typically http://localhost:5173)
  </acceptance_criteria>
  <done>Test suite green; dev server running and accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — PNG and PDF export quality</name>
  <files></files>
  <action>Human visual verification — no code changes. See how-to-verify for steps.</action>
  <verify>User confirms "approved" after checking both downloaded files.</verify>
  <done>User has visually confirmed PNG and PDF exports are correct and types "approved".</done>
  <read_first>
    - src/pages/Processen.tsx lines 204–246 (export functions, for reference if bugs arise)
  </read_first>
  <acceptance_criteria>
    - PNG downloads with toast "PNG gedownload"
    - PDF downloads with toast "PDF gedownload"
    - Both files show legible swimlane layout with automation names
    - No "Export mislukt" toast
    - User types "approved"
  </acceptance_criteria>
  <what-built>
    The export feature in Processen.tsx: an Export dropdown button in the page header that
    offers "Exporteer als PNG" and "Exporteer als PDF". Both use a pure-browser SVG-to-canvas
    pipeline. PNG downloads via an anchor click; PDF is generated by the jspdf npm package
    (bundled — no CDN) and saved as proceskaart.pdf.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:5173 in your browser and navigate to the Processen page.
    2. Wait for the canvas to fully load (automations visible in swimlanes).

    PNG test:
    3. Click the Export button (FileDown icon) in the top-right of the page header.
    4. Select "Exporteer als PNG".
    5. Confirm a toast "PNG gedownload" appears.
    6. Open the downloaded proceskaart.png file.
    7. Verify: Swimlane layout is visible (5 phases in columns or rows).
    8. Verify: Automation names are legible at 100% zoom.
    9. Verify: Swimlane header backgrounds have colour (not transparent/white/grey).

    PDF test:
    10. Click Export again and select "Exporteer als PDF".
    11. Confirm a toast "PDF gedownload" appears.
    12. Open the downloaded proceskaart.pdf file.
    13. Verify: Landscape orientation, swimlane layout matches the canvas.
    14. Verify: Automation names are legible.

    Known non-issues (do NOT flag these):
    - Minor font rendering differences between browser and export file — acceptable.
    - Slight colour variation due to screen colour profile — acceptable.

    Issues to flag:
    - Export fails (toast "Export mislukt") — report the browser console error.
    - Swimlane backgrounds are transparent/white — swimlane colours missing.
    - Automation names are unreadable (too small or garbled) — legibility failure.
    - Export is cropped (only part of the canvas) — clipping bug.
    - PDF is portrait orientation instead of landscape — orientation bug.
  </how-to-verify>
  <resume-signal>
    If both exports look correct: type "approved"
    If any issue found: describe what you see (e.g., "swimlane backgrounds transparent in PNG")
    so the bug can be diagnosed and fixed before re-running this checkpoint.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 3 is complete when:
1. npx vitest run — all tests green (automated)
2. PNG export downloads and shows legible swimlane layout with correct colours (human verified)
3. PDF export downloads as landscape with legible content (human verified)
</verification>

<success_criteria>
- User types "approved" after visually confirming both PNG and PDF exports
- Both files download without errors
- Swimlane layout and automation names legible in both formats
- No console errors during export
</success_criteria>

<output>
After completion, create .planning/phases/03-export/03-03-SUMMARY.md
</output>
