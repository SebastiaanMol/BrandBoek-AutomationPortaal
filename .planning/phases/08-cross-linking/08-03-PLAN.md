---
phase: 08-cross-linking
plan: "03"
type: execute
wave: 2
depends_on:
  - "08-02"
files_modified: []
autonomous: false
requirements:
  - LINK-01
  - LINK-02
  - LINK-03
  - LINK-04

must_haves:
  truths:
    - "User confirms all four cross-links work by clicking them in the running app"
    - "No console errors during navigation"
    - "Systems page opens filtered to the clicked system"
    - "Owners page opens filtered to the clicked owner"
    - "All Automations page opens with the related automation expanded"
  artifacts: []
  key_links: []
---

<objective>
Human smoke test of all four cross-links in the automation detail panel.

Purpose: Confirms the automated implementation from Wave 1 works end-to-end in the browser — link rendering, navigation, and filter activation cannot be verified by Vitest alone.

Output: User sign-off that LINK-01 through LINK-04 work as specified.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cross-linking/08-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Four cross-links added to the automation detail panel in AutomationDetailPanel.tsx:
    - LINK-01: "View on canvas" link to /processen
    - LINK-02: System badges are clickable links to /systems?system=X
    - LINK-03: Owner name is a clickable link to /owners?owner=X
    - LINK-04: "Related" section with up to 5 automations, each linking to /alle?open={id}
  </what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev`
2. Open the app in the browser (typically http://localhost:5173)
3. Navigate to the Processes page (sidebar: Processes)
4. Click any automation node on the canvas to open the detail panel

**LINK-01 — Canvas link:**
5. Confirm a "View on canvas" link with an external-link icon appears near the top of the panel
6. Click it — you should stay on (or navigate to) the Processes page

**LINK-02 — System badge links:**
7. In the detail panel, find the "Systemen" section
8. Click any system badge (e.g., "HubSpot")
9. Confirm you land on the Systems page (/systems) with the systems list filtered to show only automations using that system

**LINK-03 — Owner link:**
10. Return to the Processes page and open the same or a different automation
11. Find the "Beheer" section — the owner name should be styled as a link (primary color, underline on hover)
12. Click the owner name
13. Confirm you land on the Owners page (/owners) filtered to that person's automations

**LINK-04 — Related automations:**
14. Return to the Processes page and open an automation that shares a phase or system with at least one other automation
15. Find the "Related" section near the bottom of the detail panel
16. Confirm it shows automation names (up to 5) with arrow icons
17. Click one of the related automation names
18. Confirm you land on the All Automations page (/alle) with that automation row expanded/open

**Negative check:**
19. Open the detail panel for an automation that has no shared phase or system with any other automation
20. Confirm the "Related" section does not appear (no empty section heading)
  </how-to-verify>
  <resume-signal>Type "approved" if all four links work correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human sign-off covers what automated tests cannot: actual browser navigation, filter activation on Systems/Owners pages, and accordion expansion on the All Automations page.
</verification>

<success_criteria>
- All four LINK-01 through LINK-04 behaviours confirmed working in the browser
- No console errors during any of the cross-link navigations
- User types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/08-cross-linking/08-03-SUMMARY.md`
</output>
