---
phase: 08-cross-linking
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/test/crossLinking.test.ts
autonomous: true
requirements:
  - LINK-04

must_haves:
  truths:
    - "crossLinking.test.ts exists and all tests pass (stubs are not failing)"
    - "deriveRelated pure function is tested with full assertions covering shared fase, shared system, self-exclusion, and empty case"
    - "DOM-level link tests for LINK-01/02/03 are it.todo stubs — non-blocking for Wave 1"
  artifacts:
    - path: "src/test/crossLinking.test.ts"
      provides: "Test scaffold for Phase 8 cross-linking logic"
      contains: "deriveRelated"
  key_links:
    - from: "src/test/crossLinking.test.ts"
      to: "deriveRelated (inline function)"
      via: "pure logic extraction — same pattern as deriveSystemCounts in entityPages.test.ts"
      pattern: "deriveRelated"
---

<objective>
Create the Wave 0 test scaffold for Phase 8 cross-linking.

Purpose: Establish non-blocking test coverage before Wave 1 implements changes. Pure logic tests for `deriveRelated` run with full assertions. DOM-level link presence tests (LINK-01, LINK-02, LINK-03) are `it.todo` stubs.

Output: `src/test/crossLinking.test.ts` — zero failing tests, full assertions on derivation logic.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cross-linking/08-CONTEXT.md
@.planning/phases/08-cross-linking/08-VALIDATION.md

<interfaces>
<!-- Key types the executor needs. From src/lib/types.ts -->

```typescript
export type Systeem = "HubSpot" | "Zapier" | "Typeform" | "SharePoint" | "WeFact" | "Docufy" | "Backend" | "E-mail" | "API" | "Anders";
export type KlantFase = "Marketing" | "Sales" | "Onboarding" | "Boekhouding" | "Offboarding";

export interface Automatisering {
  id: string;
  naam: string;
  categorie: Categorie;
  doel: string;
  trigger: string;
  systemen: Systeem[];
  stappen: string[];
  afhankelijkheden: string;
  owner: string;
  status: Status;
  verbeterideeën: string;
  mermaidDiagram: string;
  koppelingen: Koppeling[];
  fasen: KlantFase[];
  createdAt: string;
  laatstGeverifieerd: string | null;
  geverifieerdDoor: string;
}
```

<!-- makeAutomatisering factory pattern from src/test/entityPages.test.ts -->

```typescript
function makeAutomatisering(overrides: Partial<Automatisering> = {}): Automatisering {
  return {
    id: "auto-1", naam: "Test", categorie: "HubSpot Workflow",
    doel: "Test doel", trigger: "Form submitted", systemen: [],
    stappen: [], afhankelijkheden: "", owner: "Jan", status: "Actief",
    verbeterideeën: "", mermaidDiagram: "", koppelingen: [],
    fasen: [], createdAt: "2026-01-01T00:00:00Z",
    laatstGeverifieerd: null, geverifieerdDoor: "",
    ...overrides,
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create crossLinking.test.ts scaffold</name>
  <files>src/test/crossLinking.test.ts</files>
  <action>
Create `src/test/crossLinking.test.ts` following the exact structure of `entityPages.test.ts`:

1. File header comment block (same style as entityPages.test.ts) explaining Wave 0 scaffold, requirements covered (LINK-01 through LINK-04), and the it.todo stub convention.

2. Imports: `import { describe, it, expect } from "vitest"` and `import { Automatisering, KlantFase, Systeem } from "@/lib/types"`.

3. Copy the `makeAutomatisering` factory function verbatim from entityPages.test.ts (inline copy — no import from that file).

4. Define `deriveRelated` inline (same pattern as `deriveSystemCounts` in entityPages.test.ts — pure function, no React, no hooks):
   ```typescript
   function deriveRelated(all: Automatisering[], current: Automatisering): Automatisering[] {
     return all.filter(a => {
       if (a.id === current.id) return false;
       const sharedFase = a.fasen?.some(f => current.fasen?.includes(f));
       const sharedSystem = a.systemen?.some(s => current.systemen?.includes(s));
       return sharedFase || sharedSystem;
     });
   }
   ```

5. `describe("deriveRelated", () => { ... })` with these FULL assertions (not stubs):
   - "returns empty array when no automations share fase or system" — all = [], current = makeAutomatisering(), expect result to have length 0
   - "includes automation sharing a fase" — create `current` with fasen: ["Marketing"], create `other` with fasen: ["Marketing"], expect result to include `other`
   - "includes automation sharing a system" — current with systemen: ["HubSpot"], other with systemen: ["HubSpot"], expect result to include `other`
   - "excludes the current automation (self)" — all = [current], expect result to have length 0
   - "excludes automations sharing neither fase nor system" — current with fasen: ["Marketing"], systemen: ["HubSpot"]; unrelated with fasen: ["Sales"], systemen: ["Zapier"]; expect result to have length 0
   - "includes automation sharing BOTH fase and system only once" — create overlap automation, expect it appears once in result (length 1, not 2)
   - "handles current with empty fasen and systemen" — current = makeAutomatisering() (empty arrays), other = makeAutomatisering with some fasen/systemen, expect result to have length 0

6. `describe("LINK-01: canvas link", () => { it.todo("panel renders a Link to /processen when fullData is provided") })`

7. `describe("LINK-02: system badge links", () => { it.todo("each system badge is wrapped in a Link to /systems?system=X") })`

8. `describe("LINK-03: owner link", () => { it.todo("owner text is wrapped in a Link to /owners?owner=X") })`

No React Testing Library. No jsdom. Pure Vitest only. All full-assertion tests must pass against the inline `deriveRelated` function as written.
  </action>
  <verify>
    <automated>npx vitest run src/test/crossLinking.test.ts</automated>
  </verify>
  <done>File exists, all tests pass (stubs are not failing), 7+ full assertions on deriveRelated, 3 it.todo stubs for DOM-level link tests.</done>
</task>

</tasks>

<verification>
Run `npm run test -- --run` after task completion. All 78+ existing tests must still pass. The new crossLinking.test.ts adds tests, all green.
</verification>

<success_criteria>
- `src/test/crossLinking.test.ts` exists
- `npx vitest run src/test/crossLinking.test.ts` exits 0
- `npm run test -- --run` exits 0 (no regressions)
- deriveRelated logic is tested with full assertions for all cases listed above
- LINK-01/02/03 DOM tests are it.todo stubs (non-blocking)
</success_criteria>

<output>
After completion, create `.planning/phases/08-cross-linking/08-01-SUMMARY.md`
</output>
