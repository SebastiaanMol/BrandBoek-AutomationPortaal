---
phase: 07-entity-pages
plan: 04
type: execute
wave: 2
depends_on:
  - 07-02
  - 07-03
files_modified:
  - src/components/AppLayout.tsx
  - src/App.tsx
autonomous: false
requirements:
  - SYS-01
  - SYS-02
  - OWN-01
  - OWN-02

must_haves:
  truths:
    - "Sidebar shows Systems and Owners under the 'Systems & People' group"
    - "Navigating to /systems loads Systems page without console errors"
    - "Navigating to /owners loads Owners page without console errors"
    - "Top bar label resolves to 'Systems' and 'Owners' for their respective routes (not 'Portal')"
    - "Active nav item shows the 4px left-edge indicator bar for /systems and /owners"
    - "Clicking a system row in the Systems page shows filtered automations"
    - "Clicking an owner row in the Owners page shows filtered automations"
  artifacts:
    - path: "src/components/AppLayout.tsx"
      provides: "Nav items for Systems (/systems) and Owners (/owners) in Systems & People group"
      contains: "items: []"
    - path: "src/App.tsx"
      provides: "Routes for /systems and /owners inside ProtectedRoutes"
      contains: "Route path"
  key_links:
    - from: "src/components/AppLayout.tsx navGroups"
      to: "src/pages/Systems.tsx"
      via: "url: '/systems' in items array — resolved by top bar flatMap"
      pattern: "/systems"
    - from: "src/components/AppLayout.tsx navGroups"
      to: "src/pages/Owners.tsx"
      via: "url: '/owners' in items array — resolved by top bar flatMap"
      pattern: "/owners"
    - from: "src/App.tsx ProtectedRoutes"
      to: "src/pages/Systems.tsx"
      via: "Route path='/systems' element={<Systems />}"
      pattern: "path.*systems"
    - from: "src/App.tsx ProtectedRoutes"
      to: "src/pages/Owners.tsx"
      via: "Route path='/owners' element={<Owners />}"
      pattern: "path.*owners"
---

<objective>
Wire the Systems and Owners pages into the app: add nav items to AppLayout and routes to App.tsx, then human-verify the full flow.

Purpose: Connect the Phase 7 pages to the live application. Without this plan, the pages exist but are unreachable.
Output: Updated AppLayout.tsx (nav items) + updated App.tsx (routes) + human smoke test confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-entity-pages/07-RESEARCH.md
@.planning/phases/07-entity-pages/07-UI-SPEC.md
@src/components/AppLayout.tsx
@src/App.tsx
@.planning/phases/07-entity-pages/07-02-SUMMARY.md
@.planning/phases/07-entity-pages/07-03-SUMMARY.md

<interfaces>
<!-- Key structures to modify. Extracted from codebase. -->

From src/components/AppLayout.tsx (current state — "Systems & People" group is empty):
```typescript
const navGroups: NavGroup[] = [
  { title: "Overview", items: [...] },
  { title: "Automations", items: [...] },
  {
    title: "Systems & People",
    items: [],              // <-- ADD Systems and Owners here
  },
  { title: "Analysis", items: [...] },
];
```

Current lucide-react imports in AppLayout.tsx (line 3-14):
LayoutDashboard, PlusCircle, List, GitBranch, BarChart3, Menu, LogOut,
ClipboardCheck, Settings, Download
ADD: Server, Users

From src/App.tsx — ProtectedRoutes Routes block (lines 37-49):
```tsx
<Routes>
  <Route path="/" element={<Dashboard />} />
  <Route path="/nieuw" element={<NieuweAutomatiseringPage />} />
  ...
  <Route path="/imports" element={<Imports />} />
  <Route path="*" element={<NotFound />} />  // keep this last
</Routes>
```
ADD before the `<Route path="*">` catch-all:
  <Route path="/systems" element={<Systems />} />
  <Route path="/owners" element={<Owners />} />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nav items to AppLayout.tsx and routes to App.tsx</name>
  <files>src/components/AppLayout.tsx, src/App.tsx</files>

  <read_first>
    - src/components/AppLayout.tsx — current lucide-react import line, current navGroups array, "Systems & People" group location
    - src/App.tsx — current imports block, ProtectedRoutes Routes block, position of `<Route path="*">` catch-all
    - .planning/phases/07-entity-pages/07-RESEARCH.md — Pattern 4 (route registration) and Pattern 5 (nav items), Pitfall 3 (h1 text mismatch), Pitfall 4 (top bar label)
    - .planning/phases/07-entity-pages/07-UI-SPEC.md — nav items table (title, URL, icon)
  </read_first>

  <action>
    CHANGE 1 — AppLayout.tsx:
    Add `Server, Users` to the existing lucide-react import on line 3.
    Current: `import { LayoutDashboard, PlusCircle, List, GitBranch, BarChart3, Menu, LogOut, ClipboardCheck, Settings, Download } from "lucide-react";`
    New: `import { LayoutDashboard, PlusCircle, List, GitBranch, BarChart3, Menu, LogOut, ClipboardCheck, Settings, Download, Server, Users } from "lucide-react";`

    Find the "Systems & People" navGroup entry (currently `items: []`) and replace with:
    ```typescript
    {
      title: "Systems & People",
      items: [
        { title: "Systems", url: "/systems", icon: Server },
        { title: "Owners", url: "/owners", icon: Users },
      ],
    },
    ```
    CRITICAL: Nav label "Systems" must match h1 "Systems" exactly. Nav label "Owners" must match h1 "Owners" exactly. (NAV-02 compliance)

    CHANGE 2 — App.tsx:
    Add two import lines after the existing page imports (before or after Imports import):
    ```typescript
    import Systems from "./pages/Systems";
    import Owners from "./pages/Owners";
    ```

    Add two routes inside the ProtectedRoutes `<Routes>` block, BEFORE the `<Route path="*">` catch-all:
    ```tsx
    <Route path="/systems" element={<Systems />} />
    <Route path="/owners" element={<Owners />} />
    ```

    Do NOT modify any other routes, imports, or AppLayout sections.
  </action>

  <verify>
    <automated>npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "Server, Users\|Users, Server" src/components/AppLayout.tsx` returns a match (both icons imported)
    - `grep -n '"/systems"' src/components/AppLayout.tsx` returns a match (nav item url)
    - `grep -n '"/owners"' src/components/AppLayout.tsx` returns a match (nav item url)
    - `grep -n "title.*Systems\|Systems.*title" src/components/AppLayout.tsx` returns a match
    - `grep -n "title.*Owners\|Owners.*title" src/components/AppLayout.tsx` returns a match
    - `grep -n 'import Systems' src/App.tsx` returns a match
    - `grep -n 'import Owners' src/App.tsx` returns a match
    - `grep -n 'path="/systems"' src/App.tsx` returns a match
    - `grep -n 'path="/owners"' src/App.tsx` returns a match
    - `npx vitest run` exits 0 (no regressions)
  </acceptance_criteria>

  <done>
    AppLayout.tsx has Systems and Owners nav items in "Systems & People" group. App.tsx has /systems and /owners routes. Full test suite green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke test — Systems and Owners pages end-to-end</name>

  <what-built>
    Systems and Owners pages are now reachable from the sidebar. Phase 7 is functionally complete.
  </what-built>

  <how-to-verify>
    Start the dev server: `npm run dev`

    1. SIDEBAR: Open the sidebar — confirm "Systems & People" group shows "Systems" and "Owners" items with icons.

    2. SYSTEMS LIST VIEW: Click "Systems" in the sidebar.
       - URL should be /systems
       - Top bar label should read "Systems" (not "Portal")
       - Page shows a visible h1 "Systems"
       - A list of system rows is shown, each with a SystemBadge, system name, and automation count
       - Rows are sorted: most-used system first

    3. SYSTEMS DETAIL VIEW: Click any system row.
       - URL should change to /systems?system=HubSpot (or whatever system was clicked)
       - "← Back to Systems" button appears at top
       - Page h1 becomes sr-only (invisible) — visible context comes from the system name header
       - Only automations linked to that system are shown
       - Automation cards expand on click (accordion)
       - No Edit or Delete buttons in expanded cards

    4. SYSTEMS BACK NAVIGATION: Click "← Back to Systems"
       - URL returns to /systems (no ?system= param)
       - List view is restored

    5. OWNERS LIST VIEW: Click "Owners" in the sidebar.
       - URL should be /owners
       - Top bar label shows "Owners"
       - Page shows a visible h1 "Owners"
       - Each row shows Users icon, owner name, automation count
       - Automations with no owner are not shown as a row

    6. OWNERS DETAIL VIEW: Click any owner row.
       - URL changes to /owners?owner=Jan (or whoever)
       - Only automations owned by that person are shown
       - "← Back to Owners" button appears

    7. LOADING STATE: If available, throttle network and refresh — spinner should appear before data loads.

    8. ACTIVE NAV STATE: While on /systems, confirm the "Systems" sidebar item shows the accent indicator bar. While on /owners, confirm "Owners" shows it.
  </how-to-verify>

  <resume-signal>
    Type "approved" if all 8 checks pass.
    Or describe any issues found (e.g., "top bar shows Portal on /systems", "back button missing on Owners").
  </resume-signal>
</task>

</tasks>

<verification>
Human smoke test across all 8 verification steps.
Automated: npx vitest run (full suite green after wiring).
</verification>

<success_criteria>
- "Systems" and "Owners" appear in sidebar under "Systems & People"
- /systems and /owners routes load without errors
- Top bar shows "Systems" and "Owners" respectively (not "Portal")
- List → detail navigation works via URL param
- Back button returns to list
- All 4 requirement IDs (SYS-01, SYS-02, OWN-01, OWN-02) visually confirmed
- Human verification approved
</success_criteria>

<output>
After completion, create .planning/phases/07-entity-pages/07-04-SUMMARY.md
</output>
