---
phase: 07-entity-pages
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/test/entityPages.test.ts
autonomous: true
requirements:
  - SYS-01
  - SYS-02
  - OWN-01
  - OWN-02

must_haves:
  truths:
    - "Test file exists at src/test/entityPages.test.ts with describe blocks for systemCounts, ownerCounts, and filter logic"
    - "All tests are it.todo stubs — they do not fail, so Wave 1 agents can run in parallel without red output"
    - "Test IDs map to SYS-01, SYS-02, OWN-01, OWN-02"
  artifacts:
    - path: "src/test/entityPages.test.ts"
      provides: "Test scaffold covering all four entity page requirement IDs"
      min_lines: 40
  key_links:
    - from: "src/test/entityPages.test.ts"
      to: "derivation logic in Systems.tsx and Owners.tsx"
      via: "Pure functions extracted inline in test file (same pattern as domainLogic.test.ts)"
      pattern: "it\\.todo"
---

<objective>
Create the Wave 0 test scaffold for Phase 7 entity pages.

Purpose: Establish test stubs for all four requirement IDs (SYS-01, SYS-02, OWN-01, OWN-02) before page implementation begins. Stubs use it.todo so Wave 1 agents are not blocked by red test output.
Output: src/test/entityPages.test.ts with four describe blocks and it.todo entries covering derivation and filter logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-entity-pages/07-VALIDATION.md
@.planning/phases/07-entity-pages/07-RESEARCH.md
@src/test/domainLogic.test.ts
@src/lib/types.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create entityPages.test.ts stub scaffold</name>
  <files>src/test/entityPages.test.ts</files>

  <read_first>
    - src/test/domainLogic.test.ts — copy the makeAutomatisering() factory and describe/it.todo pattern exactly
    - src/lib/types.ts — Automatisering interface and Systeem type for factory shape
    - .planning/phases/07-entity-pages/07-RESEARCH.md — systemCounts and ownerCounts derivation algorithms to reference in stub descriptions
    - .planning/phases/07-entity-pages/07-VALIDATION.md — test map (which req IDs each test covers)
  </read_first>

  <behavior>
    Wave 0 stubs ONLY — no implementations. All tests must be it.todo (not failing assertions).

    Required structure:

    describe("systemCounts derivation") — SYS-01
      it.todo("returns empty map for no automations")
      it.todo("counts each system once per automation")
      it.todo("aggregates the same system across multiple automations")
      it.todo("sorts descending by count")

    describe("system filter") — SYS-02
      it.todo("returns only automations containing the selected system")
      it.todo("returns empty array when no automations match the system")

    describe("ownerCounts derivation") — OWN-01
      it.todo("returns empty map for no automations")
      it.todo("groups automations by owner name")
      it.todo("skips automations with empty string owner")
      it.todo("sorts descending by count")

    describe("owner filter") — OWN-02
      it.todo("returns only automations whose owner matches")
      it.todo("returns empty array when no automations match the owner")

    Copy makeAutomatisering() factory from domainLogic.test.ts — identical shape.
    File header comment must reference: "Wave 0 scaffold — tests made GREEN by plans 07-02 and 07-03"
  </behavior>

  <action>
    Create src/test/entityPages.test.ts.

    File structure:
    1. Header comment block: purpose, Wave 0 note, req IDs covered (SYS-01, SYS-02, OWN-01, OWN-02)
    2. Import: `import { describe, it } from "vitest";`
    3. Import: `import { Automatisering, Systeem } from "@/lib/types";`
    4. Copy makeAutomatisering() factory verbatim from domainLogic.test.ts (lines 13-23) — same defaults
    5. Four describe blocks with it.todo stubs as specified in behavior above
    6. No test implementations — it.todo only

    Do NOT use it("...", () => { expect(...).toBe(...) }) — only it.todo("...").
    This matches the Phase 04 decision: "Used it.todo stubs (not failing assertions) for Wave 0 scaffold so parallel Wave 1 agents are not blocked by red test output."
  </action>

  <verify>
    <automated>npx vitest run src/test/entityPages.test.ts</automated>
    All tests must show as "todo" (skipped), not "failed". Exit code 0.
  </verify>

  <done>
    src/test/entityPages.test.ts exists with 12 it.todo stubs across 4 describe blocks.
    `npx vitest run src/test/entityPages.test.ts` exits 0 with all tests shown as todo/skipped.
    No import errors, no failing assertions.
  </done>
</task>

</tasks>

<verification>
Run: npx vitest run src/test/entityPages.test.ts
Expected: Exit 0, all tests shown as "todo" (12 stubs), zero failures.
</verification>

<success_criteria>
- src/test/entityPages.test.ts exists
- 4 describe blocks present: "systemCounts derivation", "system filter", "ownerCounts derivation", "owner filter"
- All tests use it.todo (no failing assertions)
- npx vitest run src/test/entityPages.test.ts exits 0
</success_criteria>

<output>
After completion, create .planning/phases/07-entity-pages/07-01-SUMMARY.md
</output>
