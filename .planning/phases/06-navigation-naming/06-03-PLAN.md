---
phase: 06-navigation-naming
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/pages/Dashboard.tsx
  - src/pages/AlleAutomatiseringen.tsx
  - src/pages/Processen.tsx
  - src/pages/Analyse.tsx
  - src/pages/Imports.tsx
  - src/pages/NieuweAutomatiseringPage.tsx
  - src/pages/Verificatie.tsx
autonomous: false
requirements:
  - NAV-02

must_haves:
  truths:
    - "Every sidebar nav item label exactly matches the text content of the <h1> on its destination page"
    - "Dashboard page has an <h1> containing 'Dashboard'"
    - "All Automations page has an <h1> containing 'All Automations'"
    - "New Automation page has an <h1> containing 'New Automation'"
    - "Verification page has an <h1> containing 'Verification'"
    - "Imports page has an <h1> containing 'Imports'"
    - "Processes page has an <h1> containing 'Processes'"
    - "Analysis page has an <h1> containing 'Analysis'"
    - "User visually confirms sidebar labels match page headings"
  artifacts:
    - path: src/pages/Dashboard.tsx
      provides: "Visible or sr-only <h1>Dashboard</h1>"
      contains: "<h1"
    - path: src/pages/AlleAutomatiseringen.tsx
      provides: "Visible or sr-only <h1>All Automations</h1>"
      contains: "<h1"
    - path: src/pages/Processen.tsx
      provides: "Visible or sr-only <h1>Processes</h1>"
      contains: "<h1"
    - path: src/pages/Analyse.tsx
      provides: "Visible or sr-only <h1>Analysis</h1>"
      contains: "<h1"
    - path: src/pages/Imports.tsx
      provides: "Visible or sr-only <h1>Imports</h1>"
      contains: "<h1"
    - path: src/pages/NieuweAutomatiseringPage.tsx
      provides: "Visible or sr-only <h1>New Automation</h1>"
      contains: "<h1"
    - path: src/pages/Verificatie.tsx
      provides: "Visible or sr-only <h1>Verification</h1>"
      contains: "<h1"
  key_links:
    - from: src/components/AppLayout.tsx
      to: "page <h1> text"
      via: "navGroups item title must be byte-identical to h1 text content"
      pattern: "h1"
---

<objective>
Add an `<h1>` element to every routed page that currently lacks one, ensuring the text is byte-identical to its sidebar nav label (as defined in the NAV-02 contract from UI-SPEC). Then confirm via human visual verification that sidebar labels and page headings match.

Purpose: NAV-02 requires sidebar nav label = page h1. Pages that have no h1 at all (Dashboard, AlleAutomatiseringen, Processen, Analyse, Imports, NieuweAutomatiseringPage) need one added. Verificatie may need its implicit heading verified. The h1 may be sr-only if the page design does not show a visible title.
Output: Every routed page has exactly one `<h1>` matching the sidebar label. Human verifies the sidebar grouping, English labels, and h1 correspondence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-navigation-naming/06-UI-SPEC.md
@.planning/phases/06-navigation-naming/06-01-SUMMARY.md
@.planning/phases/06-navigation-naming/06-02-SUMMARY.md
</context>

<interfaces>
<!-- NAV-02 canonical label table from UI-SPEC: -->
| Route        | Sidebar Label    | Required h1 text  |
|--------------|------------------|-------------------|
| /            | Dashboard        | Dashboard         |
| /alle        | All Automations  | All Automations   |
| /nieuw       | New Automation   | New Automation    |
| /verificatie | Verification     | Verification      |
| /imports     | Imports          | Imports           |
| /processen   | Processes        | Processes         |
| /analyse     | Analysis         | Analysis          |
| /instellingen| Settings         | Settings          |

<!-- sr-only pattern (use when page design has no visible page title): -->
<h1 className="sr-only">{Label}</h1>

<!-- visible heading pattern (use when adding a heading improves the page): -->
<h1 className="text-xl font-semibold tracking-tight text-foreground">{Label}</h1>

<!-- Pages that likely benefit from a visible h1 (currently have no page-level heading):
     Dashboard — has metric cards but no title; sr-only preferred (metrics are the identity)
     AlleAutomatiseringen — has filter bar but no title; sr-only preferred
     Processen — full-bleed canvas; sr-only required (no room for heading)
     Analyse — has section headings but no page h1; sr-only preferred
     Imports — has content; add visible h1 at top of page content
     NieuweAutomatiseringPage — has tabs but no page title; add visible h1 above tabs
     Verificatie — has progress bar; sr-only preferred (progress bar is the identity)
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add h1 elements to all pages missing one</name>
  <files>
    src/pages/Dashboard.tsx,
    src/pages/AlleAutomatiseringen.tsx,
    src/pages/Processen.tsx,
    src/pages/Analyse.tsx,
    src/pages/Imports.tsx,
    src/pages/NieuweAutomatiseringPage.tsx,
    src/pages/Verificatie.tsx
  </files>
  <read_first>
    src/pages/Dashboard.tsx
    src/pages/AlleAutomatiseringen.tsx
    src/pages/Processen.tsx
    src/pages/Analyse.tsx
    src/pages/Imports.tsx
    src/pages/NieuweAutomatiseringPage.tsx
    src/pages/Verificatie.tsx
  </read_first>
  <action>
For each page, add exactly ONE `<h1>` element. The text must be byte-identical to the sidebar nav label from the NAV-02 table. Use sr-only or visible heading as noted below.

**src/pages/Dashboard.tsx:**
After the loading check returns, at the very top of the non-loading return JSX (before the `<div className="space-y-8">`), add:
```tsx
<h1 className="sr-only">Dashboard</h1>
```
Wrap the existing return content and the sr-only h1 in a Fragment or add the h1 as the first child of the existing outer div.

**src/pages/AlleAutomatiseringen.tsx:**
At the top of the loaded return's outer `<div className="space-y-4">`, add as the first child:
```tsx
<h1 className="sr-only">All Automations</h1>
```

**src/pages/NieuweAutomatiseringPage.tsx:**
Above the `<Tabs>` element, add a visible heading:
```tsx
<h1 className="text-xl font-semibold tracking-tight text-foreground">New Automation</h1>
```

**src/pages/Verificatie.tsx:**
At the top of the loaded return's outer `<div className="max-w-3xl mx-auto space-y-6">`, add as the first child:
```tsx
<h1 className="sr-only">Verification</h1>
```

**src/pages/Imports.tsx:**
Read the file first. If no `<h1>` exists, add a visible heading near the top of the main content area:
```tsx
<h1 className="text-xl font-semibold tracking-tight text-foreground">Imports</h1>
```

**src/pages/Processen.tsx:**
This is a full-bleed canvas page (p-0). Add as the very first child of the component's return JSX (before the canvas wrapper):
```tsx
<h1 className="sr-only">Processes</h1>
```
Wrap in a Fragment if needed: `<><h1 className="sr-only">Processes</h1>{...existing JSX}</>`.

**src/pages/Analyse.tsx:**
At the top of the loaded return's outer div, add as the first child:
```tsx
<h1 className="sr-only">Analysis</h1>
```

Note: Do not add h1 to Instellingen.tsx or BewerkAutomatisering.tsx — those pages are not in scope for this plan (Instellingen is in bottom nav; BewerkAutomatisering has no sidebar link).
  </action>
  <verify>
    <automated>grep -rn "<h1" "/c/Users/SebastiaanMol/Desktop/Nieuwe map/automation-navigator/src/pages/Dashboard.tsx" "/c/Users/SebastiaanMol/Desktop/Nieuwe map/automation-navigator/src/pages/AlleAutomatiseringen.tsx" "/c/Users/SebastiaanMol/Desktop/Nieuwe map/automation-navigator/src/pages/Processen.tsx" "/c/Users/SebastiaanMol/Desktop/Nieuwe map/automation-navigator/src/pages/Analyse.tsx" "/c/Users/SebastiaanMol/Desktop/Nieuwe map/automation-navigator/src/pages/Imports.tsx" "/c/Users/SebastiaanMol/Desktop/Nieuwe map/automation-navigator/src/pages/NieuweAutomatiseringPage.tsx" "/c/Users/SebastiaanMol/Desktop/Nieuwe map/automation-navigator/src/pages/Verificatie.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "<h1" src/pages/Dashboard.tsx` returns exactly 1 match containing "Dashboard"
    - `grep "<h1" src/pages/AlleAutomatiseringen.tsx` returns exactly 1 match containing "All Automations"
    - `grep "<h1" src/pages/NieuweAutomatiseringPage.tsx` returns exactly 1 match containing "New Automation"
    - `grep "<h1" src/pages/Verificatie.tsx` returns exactly 1 match containing "Verification"
    - `grep "<h1" src/pages/Imports.tsx` returns exactly 1 match containing "Imports"
    - `grep "<h1" src/pages/Processen.tsx` returns exactly 1 match containing "Processes"
    - `grep "<h1" src/pages/Analyse.tsx` returns exactly 1 match containing "Analysis"
    - `npm run build` completes without TypeScript errors
  </acceptance_criteria>
  <done>Every page reachable from the sidebar has exactly one h1 whose text matches the sidebar nav label. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human verification of Phase 6 Navigation and Naming</name>
  <what-built>
    Phase 6 Navigation and Naming complete:
    - Plan 01: Sidebar restructured into 4 named groups (Overview / Automations / Systems & People / Analysis); dead pages removed
    - Plan 02: All Dutch UI strings replaced with English across Dashboard, AlleAutomatiseringen, Verificatie, AutomatiseringForm, NieuweAutomatiseringPage, Analyse; STATUS_LABELS added; CSS class names updated
    - Plan 03: h1 added to every routed page matching its sidebar nav label
  </what-built>
  <how-to-verify>
    1. Open the app in the browser (run `npm run dev` if not already running).

    2. CHECK SIDEBAR STRUCTURE (NAV-01):
       - Sidebar should show four group headers: "OVERVIEW", "AUTOMATIONS", "SYSTEMS & PEOPLE", "ANALYSIS"
       - Under Overview: Dashboard
       - Under Automations: All Automations, New Automation, Verification, Imports
       - Under Systems & People: nothing (placeholder, empty group header visible)
       - Under Analysis: Processes, Analysis
       - No ungrouped items should appear

    3. CHECK DEAD PAGES REMOVED (NAV-03):
       - Navigate to /mindmap — should see 404/NotFound page, not a Mindmap page
       - Navigate to /kennisgraaf — should see 404/NotFound
       - Navigate to /bpmn — should see 404/NotFound
       - Navigate to /proceskaart — should see 404/NotFound

    4. CHECK ENGLISH LABELS (NAME-01):
       - Dashboard: see "Total Recorded", "Active", "Outdated", "Disabled", "Verification Status", "Automations by Status", tab labels "Active / Outdated / Disabled", button "+ New Automation"
       - All Automations: search placeholder "Search all fields...", filters "All categories / All systems / All statuses", button "Download CSV", actions "Edit / Delete"
       - Verification: tab labels "Verification / All / Verified / Outdated / In Review", progress "of N verified", keyboard hint "→ approve · ← outdated · ↑ doubt · ↓ skip", buttons "Verified / Uncertain / Outdated / Edit / Skip"
       - New Automation form: field labels "Name / Category / Goal / Trigger / Primary Systems / Customer Process Phase / Flow / Steps / Dependencies / Owner / Status / Improvement Ideas / Links / Flow Diagram (Mermaid)", button "Save Automation"
       - Sign out button in sidebar: "Sign out" (not "Uitloggen")
       - Brand header: "Automation Portal" (not "Automatisering Portaal")

    5. CHECK NAV-LABEL = H1 (NAV-02):
       - Click each nav item and check the top bar label matches the page's main heading
       - Dashboard: top bar shows "Dashboard"
       - All Automations: top bar shows "All Automations"
       - New Automation: top bar shows "New Automation", visible h1 "New Automation" above the tabs
       - Verification: top bar shows "Verification"
       - Imports: top bar shows "Imports"
       - Processes: top bar shows "Processes"
       - Analysis: top bar shows "Analysis"

    6. CHECK STATUS BADGES:
       - On any page showing automations, status badges should say "Active", "Outdated", "Disabled", "In Review" (not Dutch)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which items failed.</resume-signal>
  <files>N/A — human verification only</files>
  <action>Follow the how-to-verify checklist above. Navigate each page and confirm sidebar grouping, English labels, dead page removal, and h1 compliance.</action>
  <verify>
    <automated>MISSING — this is a human verification checkpoint; no automated command applies</automated>
  </verify>
  <done>User types "approved" after confirming all six checklist items pass.</done>
</task>

</tasks>

<verification>
All phase 6 requirements satisfied when:
- Sidebar shows 4 named group headers, all items correctly grouped
- /mindmap /kennisgraaf /bpmn /proceskaart return 404
- Every visible string in the UI is English
- Every page reachable from sidebar has an h1 matching the sidebar label
- npm run build passes
</verification>

<success_criteria>
1. Sidebar organized under Overview / Automations / Systems & People / Analysis group headers
2. No route leads to dead pages
3. All UI labels are English — no Dutch text visible
4. Every routed page has exactly one h1 matching its sidebar nav label
5. Human verifier approves all checks
</success_criteria>

<output>
After completion, create `.planning/phases/06-navigation-naming/06-03-SUMMARY.md`
</output>
